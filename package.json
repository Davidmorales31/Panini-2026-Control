{
  "name": "mi-album-mundialista-2026",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "web": "node dev-server.mjs",
    "api": "node backend/server.mjs",
    "build": "node scripts/build-static.mjs",
    "check": "node --check app.js && node --check sw.js && node --check backend/server.mjs",
    "check:cloudflare": "node --check cloudflare/worker.js",
    "android:sync": "npm run build && npx cap sync android",
    "android:open": "npm run build && npx cap open android",
    "android:notes": "node -e \"console.log('Instala Capacitor y Android Studio. Lee docs/android-build.md')\""
  },
  "devDependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  }
}
